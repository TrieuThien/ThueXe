import { Router } from "express";
// import { registerUser, loginUser, loginAdmin } from "../controllers/userController.mjs";
// import { adminAuth, userAuth } from "../middleware/authMiddleware.mjs";

const router = Router();

// const routeValue = "api/user/";

// router.post(`${routeValue}/register`, registerUser);
// router.post(`${routeValue}/login`, userAuth, loginUser);

// // For admin login
// router.post("/admin/login", adminAuth, loginAdmin);

export default router;